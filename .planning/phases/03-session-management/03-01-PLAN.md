---
phase: 03-session-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/pages/Home.tsx]
autonomous: true
---

<objective>
Implement home page as the entry point for starting a bill split session.

Purpose: Allow hosts to create a new session by entering their name and starting the flow.
Output: Functional Home page that creates sessions and navigates to the Session page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-session-management/03-CONTEXT.md

@src/pages/Home.tsx
@convex/sessions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Home page with session creation</name>
  <files>src/pages/Home.tsx</files>
  <action>
Replace the placeholder Home page with a functional entry point:

1. Add state for hostName input
2. Display:
   - App title and tagline
   - "Your name" input field (required)
   - "Start splitting" button (disabled if name empty)
3. On button click:
   - Call sessions.create mutation with hostName
   - Navigate to /session/:code using returned code
4. Use useMutation hook for sessions.create
5. Use useNavigate from react-router-dom

Styling: Mobile-first, centered content, large tap targets for buttons, input with proper mobile keyboard support.
  </action>
  <verify>npm run dev, visit localhost:5173, enter name, click Start splitting, verify redirect to /session/:code</verify>
  <done>Home page creates session and navigates to Session page with correct code</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Home page displays name input and Start button
- [ ] Clicking Start creates session and navigates to /session/:code
- [ ] Session page shows the created session
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Home page properly creates sessions and navigates
</success_criteria>

<output>
After completion, create `.planning/phases/03-session-management/03-01-SUMMARY.md`
</output>

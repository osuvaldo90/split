---
phase: 10-feature-enhancements
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Session.tsx
  - src/pages/Join.tsx
  - src/components/Summary.tsx
  - src/components/TabNavigation.tsx
autonomous: true
---

<objective>
Rename "session" terminology to "bill" throughout user-facing UI.

Purpose: "Bill" is more intuitive for users than "session" when splitting restaurant bills.
Output: All user-visible text uses "bill" instead of "session".
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-feature-enhancements/10-CONTEXT.md

@src/pages/Session.tsx
@src/pages/Join.tsx
@src/components/Summary.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename user-facing text in Session page</name>
  <files>src/pages/Session.tsx</files>
  <action>
    Search and replace user-visible text only (keep code variable names as-is):

    1. Error messages:
       - "Session not found" → "Bill not found"
       - "Unable to find session" → "Unable to find bill"
       - Any other session-related error text

    2. UI labels:
       - "Share code:" text should remain simple (just "Share code: ABC123" per context)
       - Any "session" text in headings or descriptions

    3. Loading states:
       - "Loading session..." → "Loading bill..."

    DO NOT change:
    - Variable names (session, sessionId, etc.)
    - Function names
    - Import paths
    - Code comments (unless user-visible)
  </action>
  <verify>npm run dev - visit session page, check error messages use "bill"</verify>
  <done>Session.tsx user-facing text uses "bill" terminology</done>
</task>

<task type="auto">
  <name>Task 2: Rename user-facing text in Join page</name>
  <files>src/pages/Join.tsx</files>
  <action>
    Update user-visible text:

    1. Page heading/instructions:
       - "Join a session" → "Join a bill" (if present)
       - "Enter session code" → "Enter bill code"

    2. Error messages:
       - "Session not found" → "Bill not found"
       - "Invalid session code" → "Invalid bill code"

    3. Input placeholders:
       - "Session code" → "Bill code"

    4. Button text:
       - "Join session" → "Join bill" (if present)

    DO NOT change variable names or code structure.
  </action>
  <verify>npm run dev - visit /join, check all text uses "bill"</verify>
  <done>Join.tsx user-facing text uses "bill" terminology</done>
</task>

<task type="auto">
  <name>Task 3: Rename text in Summary and other components</name>
  <files>src/components/Summary.tsx, src/components/TabNavigation.tsx</files>
  <action>
    1. In Summary.tsx:
       - Check for any "session" text in labels or messages
       - Update to "bill" where user-visible

    2. In TabNavigation.tsx (if exists):
       - Check tab labels for "session" references
       - Update to "bill" where appropriate

    3. General scan of components for any remaining user-visible "session" text:
       - Error messages
       - Empty states
       - Tooltips or help text

    Keep share code display simple: "Share code: ABC123" (no "Bill" prefix).
  </action>
  <verify>npm run dev - navigate through app, verify consistent "bill" terminology</verify>
  <done>All user-facing components use "bill" instead of "session"</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run dev` works
- [ ] Session page shows "Bill not found" for invalid codes
- [ ] Join page uses "bill" terminology
- [ ] Share code displays simply as "Share code: ABC123"
- [ ] No user-visible "session" text remains
</verification>

<success_criteria>
- All tasks completed
- Consistent "bill" terminology in UI
- Code structure unchanged (only user-facing strings)
- Share code format is simple (no "Bill" prefix)
</success_criteria>

<output>
After completion, create `.planning/phases/10-feature-enhancements/10-03-SUMMARY.md`
</output>
